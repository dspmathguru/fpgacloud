#!/usr/bin/env python
# Stop the instance that this command is run in
#    Assumes you have permission through a role to do this

import json
import MyMetaData

if __name__ == "__main__":
  myMetaData = MyMetaData.MyMetaData()
